spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/cityguide
    username: postgres
    password: mypassword123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8090
